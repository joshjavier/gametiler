export interface GameMetaData {
  game: string;
  name: string;
  provider: string;
  sid: string;
  ownerId: string;
}

export interface Game {
  id: string;
  game: string;
  name: string;
  provider: string;
}
